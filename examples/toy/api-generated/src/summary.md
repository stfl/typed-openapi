<!--
Generated by `cargo run -p xtask -- bless` from toy.yaml, corrections.yaml and cli.yaml.
Do not edit: every number below is counted off that document's reduction, and
that command counts them again.
-->

# What the reduction found

Every number here is counted off the reduced model itself — the same value a shipped
binary loads — so a number that has moved is a document that has moved, and a page
quoting one is quoting a measurement rather than a memory.

| what | how many |
|---|---|
| operations | 12 |
| groups they are mounted under | 5 |
| reads, sent on sight | 2 |
| writes, held behind `--commit` | 10 |
| operations the document asks no body of | 5 |
| bodies that go out whole, with no flag per property | 3 |
| parameters carried without a flag | 0 |

## Groups

`vouchers`, `contacts`, `documents`, `documents-multipart`, `ledger`

## Gates

| gate | operations behind it |
|---|---|
| `--email` | 1 |
| `--enshrine` | 1 |

## Parameters carried without a flag

None: every parameter the document declares reaches the request.
